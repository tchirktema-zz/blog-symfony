{% extends 'base.html.twig' %}

{% block title %}Registration
{% endblock %}

{% block body %}
    {{ form_start(form) }}
        {{ form_errors(form) }}
        {{ form_rest(form) }}
        <button type="submit">Register</button>
    {{ form_end(form) }}
{% endblock %}
